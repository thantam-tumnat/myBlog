# Frontend API endpoints — Vite จะ expose เฉพาะตัวแปรที่ขึ้นต้น VITE_ ให้ browser
# ก๊อปไฟล์นี้เป็น .env แล้วแก้ค่าตามสภาพแวดล้อม (prod ใส่ domain จริง)
VITE_USER_API=http://localhost:8001/v1/myblogs
VITE_BLOG_API=http://localhost:8002/v1/myblogs
